import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("This is Swift App")
            Text("Hello, World")
            Text("Hello, World")
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("This is Swift App")
            Text("This is Swift App")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
